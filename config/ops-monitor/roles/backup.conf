# Backup role configuration for ae_provisioning
# This file is used to identify the backup role and can be extended for backup-specific settings.
ROLE=backup
BACKUP_ROOT="/mnt/Backups"
# Add more backup-specific configuration as needed.
