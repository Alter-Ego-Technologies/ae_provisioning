# Web overlay - LiteSpeed + CyberPanel (host-based, no containers)
# Core CyberPanel services
SERVICES="${SERVICES} lscpd lsws mariadb postfix dovecot named pure-ftpd"

# No Docker containers (CyberPanel runs natively)
CONTAINERS=""

# Optional: uncomment if using Redis
# SERVICES="${SERVICES} redis-server"

