# Web overlay

# Monitor docker as a host service (it should always be running on this node now)
SERVICES="${SERVICES} docker containerd"

# No web containers deployed yet (add once docker ps shows your stack)
CONTAINERS=""
